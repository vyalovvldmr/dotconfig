name: Darwin Nix

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CI: 1
  NONINTERACTIVE: 1
    
jobs:
  install-on-mac:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - uses: extractions/setup-just@v2
    - name: Test Install
      run: just install
